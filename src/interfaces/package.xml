<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">

  <name>interfaces</name>
  <version>0.0.0</version>
  <description>
    Defines the custom Navigate action used in the ROS2 navigation system.
    The action allows a client to send a target pose (x, y, theta) to the
    server, which drives the robot to the goal and reports distance feedback.
  </description>

  <maintainer email="matte.kraft@gmail.com">maintainer</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <exec_depend>rosidl_default_runtime</exec_depend>

  <depend>action_msgs</depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>

</package>
